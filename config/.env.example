# Environment Variables Example for Docker Deployment
# Copy this file to .env and customize for your environment

# ============================================================================
# Server Configuration
# ============================================================================
SITE_URL=http://localhost:8080
TRUSTED_PLATFORM=

# API Key (REQUIRED)
# Generate with: openssl rand -base64 32
API_KEY=your-secret-api-key-change-me

# ============================================================================
# Admin Account
# ============================================================================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password-change-me

# ============================================================================
# Database Configuration
# ============================================================================
# Database type: sqlite, postgres, or mysql
DATABASE_TYPE=postgres

# PostgreSQL Configuration
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=shortener
DATABASE_USER=shortener
DATABASE_PASSWORD=your-postgres-password-change-me
DATABASE_SSLMODE=disable

# MySQL Configuration (if using MySQL)
# DATABASE_HOST=mysql
# DATABASE_PORT=3306
# DATABASE_NAME=shortener
# DATABASE_USER=shortener
# DATABASE_PASSWORD=your-mysql-password-change-me

# ============================================================================
# Cache Configuration
# ============================================================================
CACHE_TYPE=redis

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password-change-me

# Valkey Configuration (if using Valkey)
# VALKEY_HOST=valkey
# VALKEY_PORT=6379
# VALKEY_USERNAME=
# VALKEY_PASSWORD=your-valkey-password-change-me

# ============================================================================
# Logging Configuration
# ============================================================================
LOG_LEVEL=info
RUST_LOG=info

# ============================================================================
# Docker Compose Configuration
# ============================================================================
# PostgreSQL
POSTGRES_USER=shortener
POSTGRES_PASSWORD=your-postgres-password-change-me
POSTGRES_DB=shortener

# MySQL (if using MySQL)
# MYSQL_ROOT_PASSWORD=your-mysql-root-password-change-me
# MYSQL_DATABASE=shortener
# MYSQL_USER=shortener
# MYSQL_PASSWORD=your-mysql-password-change-me
